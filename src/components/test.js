const anArray = [1, 2, 3, 4];

const anObject = {
    myName: 'zach',
    myAge: 31,
    skills: {
        funStuff: ['drawing', 'videogames'],
        workStuff: ['typing']
    }
}

let clone = {...anObject}
